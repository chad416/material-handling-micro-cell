# OPC UA Certificates

Generated certificate and private-key files live here for local testing.
They are intentionally ignored by Git.

Use:

```powershell
python -m opcua_server.generate_cert --hostname localhost
```
